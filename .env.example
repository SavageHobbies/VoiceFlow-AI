# API Keys for WinAssistAI
# Remove .example from the filename and fill in your API keys.

# --- ElevenLabs Voice Synthesis ---
# Get your API key from https://elevenlabs.io/
ELEVENLABS_API_KEY=YOUR_ELEVENLABS_API_KEY_HERE

# --- Conversational AI Settings ---
# The AI_PROVIDER in this .env file can serve as a fallback if config/ai_config.json is missing or invalid.
# The primary configuration for AI provider and model selection is now in config/ai_config.json.
# See that file and the set-ai-provider.ps1 script for more details.
AI_PROVIDER=OpenAI 
# The API Key for the selected provider is still sourced from this .env file.
# For OpenAI (if it's your default or selected provider in ai_config.json):
OPENAI_API_KEY=YOUR_OPENAI_API_KEY_HERE
# For other providers, you would add their respective API key env variables here, e.g.:
# GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE 

# --- Serenade Wake Word ---
# This is used by the serenade_custom_script_template.js
# Note: If you change this, you'll need to update it in your copy of the Serenade script too.
WAKE_WORD=COMPUTER
